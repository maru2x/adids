## ToDOリスト

- make run時の結果出力ディレクトリのパスを設定できるようにしたい 現状は exp/　以下に流れている
- 特徴量抽出zeekモードで、入出力パスの指定が不自然
- stモードの出力でモデルが複数できている？
- elk stackの再構築
- make venvとはなんぞや
- tmuxのコピーリング同期の不具合を治す
- jjでescしなくね
- combinerの挙動修正、挙動確認
- 研究基盤上のemacs -nwでemacs系列の移動系ショートカットが利用できない
  - stash popしてある変更に残っているかも。前に一度完了したはずができなくなってしまった。

## 取組中のタスクメモ

### Zeekによるフロー特徴量の検証

splitモードの動作確認を行う。
そのためにデータセットを用意する必要がある。
良性データセットと悪性データセットの結合に苦戦中。
また、stモードで実行してもなぜかモデルが複数生成される様子が確認された。
その挙動も確認する必要がある。

### トラヒックデータ作成に関する導線に問題がないか、実際のデータを使って確認

実際のデータを使って確認する。

### トラヒックデータの作成方法をドキュメント化する


## 完了したこと

| タスク名 | 完了日 |
| gst などをターミナルで実行したときに日本語が表示されない | 2026.04.27以前 |
| dotfilesのset-linkの挙動修正 | 2026.04.27 |
| combinerの挙動修正 | 2026.04.27 |
|  | 2026.04.27 |
|  | 2026.04.27 |

## 完了タスクメモ

### conbinerの挙動修正

中身は大きく変えず、入出力だけsettings.jsonから触れるようにした。
動作が若干怪しいところもあるので、次回研究時はここの確認を行う
実装、検証環境でのテストは完了。

### dotfiles

set-linkの挙動で、 rm-rfが危険らしい。この部分の修正を行う必要がある。

