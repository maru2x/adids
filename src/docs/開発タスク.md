## ToDOリスト

- make run時の結果出力ディレクトリのパスを設定できるようにしたい 現状は exp/　以下に流れている
- 特徴量抽出zeekモードで、入出力パスの指定が不自然
- stモードの出力でモデルが複数できている？
- elk stackの再構築
- make venvとはなんぞや

## 取り組んでいること

### Zeekによるフロー特徴量の検証

splitモードの動作確認を行う。
そのためにデータセットを用意する必要がある。
良性データセットと悪性データセットの結合に苦戦中。
また、stモードで実行してもなぜかモデルが複数生成される様子が確認された。
その挙動も確認する必要がある。

### conbinerの挙動修正

## 完了したこと

- gst などをターミナルで実行したときに日本語が表示されない
- tmuxのコピーリング同期の不具合を治す
- conbinerの挙動を治す
- トラヒックデータをどうやって作成していたのか思い出す。
- 研究基盤上のemacs -nwでemacs系列の移動系ショートカットが利用できない
