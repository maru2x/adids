`src/test/manual/` is for ad-hoc scripts, notebooks, and local artifacts that support manual verification.

Files in this directory are not part of `make docs-check` or `make test`. Keep automated tests in `src/test/` as `test_*.py`.
