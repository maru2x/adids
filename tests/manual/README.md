`tests/manual/` is for ad-hoc scripts, notebooks, and local artifacts that support manual verification.

Files in this directory are not part of `make docs-check`, `make test`, or `make test-e2e`. Keep automated tests under `tests/unit/` or `tests/e2e/`.
